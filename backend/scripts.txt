CREATE TABLE USERS (
userId number,
userType varchar(1),
userName varchar(25),
userEmail varchar(60),
userPassword varchar(15)
)

CREATE TABLE CONTACT_DETAILS(
contactId number,
primaryPhoneNo varchar(10),
secondaryPhoneNo varchar(10),
primaryAddress varchar(60),
shippingAddress varchar(60)
)

CREATE TABLE PRODUCTS (
productId number,
productName varchar(15),
productDesc varchar(30),
price number not null,
photoId number,
totalQuantity number,
userId number,
brandId number,
inStock number,
productType varchar(1)
)

CREATE TABLE WISH_LIST_ITEM (
wishListItemId number,
productName varchar(30),
userId number
)

CREATE TABLE CART_ITEM(
cartItemId number,
quantity number,
productPrice number(10,2),
userId number,
productName varchar(30)
)

---------------------------------

CREATE TABLE PRODUCT_TYPE(
productTypeId varchar(1),
productTypeName varchar(15)
)

CREATE TABLE BRAND (
brandId number,
brandName varchar(20),
brandDesc varchar(30)
)

CREATE TABLE RETAILER(
userId number,
total_revenue number
)



CREATE TABLE PHOTO(
productId number,
photoId number,
photoNo1 blob,
photoNo2 blob,
photoNo3 blob,
photoNo4 blob)

CREATE TABLE MOBILES(
productId number,
ram varchar(10),
rom varchar(10),
camera varchar(10),
resolution varchar(15),
battery number,
sim varchar(30),
processor varchar(20),
os varchar(10),
color varchar(10)
)

CREATE TABLE LAPTOPS(
productId number,
ram varchar(10),
rom varchar(10),
camera varchar(10),
resolution varchar(15),
battery number,
USB_slots number,
processor varchar(20),
os varchar(10),
color varchar(10)
)

CREATE TABLE REVIEW_DETAILS(

reviewDetailId number,
reviewDetail varchar(20),
rating number,
userId number,
productId number
)



CREATE TABLE PAYMENTS(
paymentId number,
paymentAmount number(10,2),
paymentType varchar(4),
paymentStatus varchar(30),
userId number,
paymentDate DATE,
paymentTime TIMESTAMP
)

CREATE TABLE PHOTO_DETAILS(
photoNo number,
photoName varchar(15),
photFormat varchar(5),
photoSrc varchar(50)
)